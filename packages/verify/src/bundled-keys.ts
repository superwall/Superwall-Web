// GENERATED FILE — do not edit by hand.
//
// Public signing keys bundled at release time, keyed by `kid`. Regenerated from
// the live JWKS on each rotation: `bun run sync-keys` (see scripts/sync-keys.ts).
//
// Empty until the first key set is published by the signer (`paywall-next`).
// Until then every token resolves via the remote JWKS fallback — correct, just
// not the zero-network fast path.

import type { JWK } from "jose";

export const BUNDLED_JWKS: JWK[] = [];
