{
  "name": "@superwall/verify",
  "version": "0.1.3",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "echo 'no-op for v0; consumers import .ts directly via Bun/Vite/Next ESM'",
    "sync-keys": "bun run scripts/sync-keys.ts",
    "clean": "rm -rf node_modules .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
