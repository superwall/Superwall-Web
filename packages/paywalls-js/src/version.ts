// Single source of truth for the SDK's published version.
// Re-exported from the package barrel; consumed directly by internal modules
// (which can't import from the barrel without a cycle).
export const SDK_VERSION = "0.0.0";
